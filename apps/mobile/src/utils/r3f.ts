/**
 * React Three Fiber re-exports — Web runtime (default / fallback).
 * Expo resolves this file when running on web (expo start --web).
 */
export { Canvas, useFrame, useThree } from '@react-three/fiber';
