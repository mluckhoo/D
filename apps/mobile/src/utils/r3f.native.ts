/**
 * React Three Fiber re-exports — React Native runtime.
 * Expo resolves this file on iOS/Android via the .native.ts extension.
 */
export { Canvas, useFrame, useThree } from '@react-three/fiber/native';
